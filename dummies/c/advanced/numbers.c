#include <stdio.h>

int main(void)
{
	int number = 0b110010;
	
	printf("Number in decimal: %d\n", number);
	printf("Number in hex: %x\n", number);
	printf("Number in octal: %o\n", number);
	
	return 0;
}