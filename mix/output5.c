// Find the output of the following proram

#include <stdio.h>

int main(void)
{
	//int i = 4, j = -1, k = 0, w, x, y, z;
	
	//w = i || j || k ;
	//x = i && j && k ;
	//y = i || j && k ;
	//z = i && j || k ;
	
	//printf ("w = %d x = %d y = %d z = %d\n", w, x, y, z ) ;

	//int i = -3, j = 3 ;
	
	//if ( !i + !j * 1 )
		//printf ( "\nMassaro" ) ;
	//else
		//printf ( "\nBennarivo" ) ;
		
	int code, flag ;
	
	if ( code == 1 & flag == 0 )
		printf ( "\nThe eagle has landed" );
		
	return 0;
}

