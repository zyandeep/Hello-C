// Find the output of the program

#include <stdio.h>

int main()
{
	//float a = 5, b = 2;
	//int c, d;
	
	//c = a % b;    	// syntax error: modulus operator only works with integer
	//d = a / 2;
	//printf("d = %d\n", d);
	
	int a = 10, b = 12, c = 0;
	
	printf("%i\n", !(a > 5 && c));
	printf("%i\n", (5 && c != 8 ) || !c);
	 
	return 0;
}

